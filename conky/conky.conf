-- Ghost-prevention stub: exits immediately without showing a window.
-- The real system panel lives in ~/.config/conky/system/conky.conf
conky.config = {
    total_run_times  = 1,
    out_to_x         = false,
    out_to_wayland   = false,
    out_to_console   = false,
}
conky.text = [[]]
